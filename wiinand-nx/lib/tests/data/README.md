# Reference files

`SYSCONF` here was written by wiicompiled-nx's own first-run code
(`runtime/include/nand_first_run.h`), so the test checks this library against
what the runtime actually produces rather than against itself.

It contains no Nintendo data: it is the settings block a blank Wii would have,
generated from our own source.
