schema_version: 1
workspace_root: .

# Punch Out. Generated by libdol-nx tools/wiinx-new-game from the disc.
project:
  id: powii-r7pe01
  display_name: Punch Out
  game_id: R7PE01            # NTSC-U, revision 1
  region: E

memory:
  base: 0x80000000
  size: 0x01800000
  sda_base: 0x804178A0
  sda2_base: 0x8041C8C0

translation:
  # Where this game's functions start, found in its own code by
  # `wiinx-scan functions`: this game has no published symbol map.
  function_map:
    path: functions.map
  entry_points:
    - 0x80006124

inputs:
  dol:
    path: disc/sys/main.dol
    sha256: 8b166ed3d9a56df11eebb40204b87c98148cac20f656ba0c10ec9c3d0977c861

  # First pass: no unsupported instructions, so gaps surface immediately.
  # (translation.entry_points above comes from the DOL header.)

# Where libdol-nx is, relative to this folder: the natives translated
# calls bind to. Change these two if you move either checkout.
runtime:
  native_registration_root: ../../libdol-nx/src
  native_abi_directories:
    - ../../libdol-nx/src/platform/gx
  # Written by wiinx-scan: which address each native has in this game.
  native_bindings: bindings.json

output:
  root: generated
  functions: functions
  runtime_config: RuntimeConfig.h
  data_initializer: data_sections_init.cpp
  base_manifest: build/base/powii_base_manifest.json
